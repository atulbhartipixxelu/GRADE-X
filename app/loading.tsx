export default function Loading() {
  return (
    <div className="grid min-h-[50vh] place-items-center">
      <p className="font-mono text-[11px] tracking-[0.32em] text-gold uppercase">Loading</p>
    </div>
  );
}
